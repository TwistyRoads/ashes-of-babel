{
  "name": "ashes-of-babel-fracture-point",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A cinematic branching narrative from the Harvest Knights universe.",
  "scripts": {
    "build:story": "inkjs-compiler src/ashes_of_babel_fracture_point.ink -o story/story.json",
    "audit": "node scripts/audit-assets.mjs",
    "test": "node tests/story-smoke.mjs && node tests/layout-contract.mjs && node tests/telemetry-client.mjs && node tests/telemetry-worker.mjs && npm run audit",
    "build": "npm run build:story && npm test"
  },
  "devDependencies": {
    "inkjs": "2.4.0"
  }
}
